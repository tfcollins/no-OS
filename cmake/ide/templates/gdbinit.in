# GDB init generated by no-OS CMake.
# STAND-IN template for the (uncommitted) cmake/ide/templates/gdbinit.in.
# Consumed via configure_file(@ONLY) by cmake/FlashTools.cmake.

target extended-remote :@GDB_PORT@
monitor reset halt
load
monitor reset init
